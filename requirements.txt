# 核心依赖
pandas>=2.0.0              # 高性能数据处理（新增，用于性能优化）
openpyxl>=3.1.0            # Excel 读写（当前使用，保持兼容）
requests>=2.31.0           # HTTP 客户端（飞书 API）

# 模板引擎
jinja2>=3.1.0              # HTML 模板引擎
# tqdm>=4.65.0             # 进度条（未来可选）

# Python 版本要求
# Python >= 3.9
